server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wx?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456  # ← 按你的数据库密码改
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none    # 我们不自动改动表结构
    show-sql: true
    properties:
      hibernate.format_sql: true

  jackson:
    time-zone: Asia/Shanghai
